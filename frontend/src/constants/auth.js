/**
 * Canonical Authentication Constants
 * ─────────────────────────────────────────────────────────────────
 * Single source of truth for OTP length and auth constraints.
 */
export const OTP_LENGTH = 8;
export const OTP_REGEX = /^\d{8}$/;
